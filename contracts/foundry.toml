[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
# Fill in the current Monad mainnet RPC from https://docs.monad.xyz before use.
monad_mainnet = "${MONAD_RPC_URL}"
